# FastAPI Backend Dependencies
# =============================
# Install with: pip install -r requirements-api.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic[email]>=2.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Authentication dependencies (JWT)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database migration
alembic>=1.13.0
psycopg[binary]>=3.2.0

# Sprint 11 — ML pipeline (CPU variants for CI/local; see requirements-ml.txt for GPU)
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
