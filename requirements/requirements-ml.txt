# Sprint 11 — Heavy ML dependencies
# ===================================
# Install separately from the main API requirements to keep the base image lean.
# Reference from requirements-api.txt only when ML training is needed.
#
# CPU-only pins for CI (no CUDA wheels):
#   pip install -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# Usage: pip install -r requirements/requirements-api.txt -r requirements/requirements-ml.txt

xgboost>=2.0.0                 # XGBoost learning-to-rank (CPU + optional CUDA)
shap>=0.45.0                   # SHAP explainability values
py-irt>=0.4.0                  # IRT 2PL/3PL Bayesian fitter (PyTorch backend)
joblib>=1.3.0                  # Model serialisation (StandardScaler, etc.)
